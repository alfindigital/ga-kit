# GAKit Environment Variables
# Copy this file to .env and fill in values as needed.
# GAKit is fully client-side — no env vars are required for basic local development.

# Optional: override the public app URL used in OG/meta tags (set at build time via Vite)
# VITE_APP_URL=https://your-domain.com
